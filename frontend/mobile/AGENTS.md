# Frontend Mobile — Flutter (Futuro)

Aplicación móvil para la calculadora de filtros eléctricos. Consume la misma API Go.

## Estado

**Pendiente de implementación.** Este directorio es un placeholder para la app Flutter futura.

## Notas

- Consumirá los mismos endpoints REST que el frontend web
- La API Go en `internal/` no requiere cambios para soportar mobile
- Documentar skills de Flutter aquí cuando se inicie el desarrollo

## Comandos (cuando se implemente)

```bash
flutter run
flutter build apk
flutter test
```
